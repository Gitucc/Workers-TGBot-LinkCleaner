{
  "private": true,
  "name": "tgbot-link-cleaner",
  "version": "1.0.0",
  "description": "TGBot LinkCleaner",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'",
    "gen-config": "node scripts/generate-config.js",
    "deploy": "npm run gen-config && wrangler deploy"
  },
  "author": "YJBeetle <yjbeetle@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2",
    "wrangler": "^4.54.0"
  }
}
